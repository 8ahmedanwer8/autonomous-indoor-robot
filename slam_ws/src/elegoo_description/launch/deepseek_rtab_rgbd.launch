<launch>
  <!-- ========= RealSense (unchanged) ========= -->
  <arg name="camera_name" default="camera"/>
  <arg name="align_depth" default="true"/>
  <arg name="publish_tf" default="true"/>
  <arg name="publish_odom_tf" default="false"/>
  <arg name="tf_publish_rate" default="30"/>

  <include file="$(find realsense2_camera)/launch/rs_camera.launch">
    <arg name="camera"          value="$(arg camera_name)"/>
    <arg name="align_depth"     value="$(arg align_depth)"/>
    <arg name="publish_tf"      value="$(arg publish_tf)"/>
    <arg name="publish_odom_tf" value="$(arg publish_odom_tf)"/>
    <arg name="tf_publish_rate" value="$(arg tf_publish_rate)"/>
  </include>

  <!-- ========= RTAB-Map ========= -->
  <group ns="rtabmap">
    <arg name="rgb_topic"         default="/camera/color/image_raw"/>
    <arg name="depth_topic"       default="/camera/aligned_depth_to_color/image_raw"/>
    <arg name="camera_info_topic" default="/camera/color/camera_info"/>

    <arg name="frame_id"      default="base_link"/>
    <arg name="odom_frame_id" default="odom"/>
    <arg name="map_frame_id"  default="map"/>

    <arg name="approx_sync"     default="true"/>
    <arg name="queue_size"      default="30"/>
    <arg name="sync_queue_size" default="30"/>

    <!-- rgbd_sync (unchanged) -->
    <node pkg="rtabmap_ros" type="rgbd_sync" name="rgbd_sync" output="screen">
      <param name="approx_sync" value="$(arg approx_sync)"/>
      <param name="queue_size" value="$(arg queue_size)"/>
      <param name="sync_queue_size" value="$(arg sync_queue_size)"/>
      <remap from="rgb/image"       to="$(arg rgb_topic)"/>
      <remap from="depth/image"     to="$(arg depth_topic)"/>
      <remap from="rgb/camera_info" to="$(arg camera_info_topic)"/>
    </node>

    <!-- RTAB-Map with added tuning -->
    <node pkg="rtabmap_ros" type="rtabmap" name="rtabmap" output="screen"
          args="--delete_db_on_start">
      <param name="frame_id" value="$(arg frame_id)"/>
      <param name="odom_frame_id" value="$(arg odom_frame_id)"/>
      <param name="map_frame_id" value="$(arg map_frame_id)"/>

      <param name="subscribe_rgbd" value="true"/>
      <remap from="rgbd_image" to="rgbd_image"/>
      <remap from="odom" to="/wheel_odom"/>

      <param name="publish_tf" value="true"/>
      <param name="publish_map_tf" value="true"/>
      <param name="tf_delay" value="0.10"/>
      <param name="wait_for_transform" value="true"/>
      <param name="wait_for_transform_duration" value="0.5"/>

      <!-- ===== New / changed parameters ===== -->
      <!-- Faster map building -->
      <param name="Rtabmap/DetectionRate" value="3.0"/>

      <!-- Fuse wheel odometry with visual odometry -->
      <param name="odom_sensor_sync" value="true"/>

      <!-- Only create a new node after sufficient visual motion -->
      <param name="RGBD/LinearUpdate"  value="0.1"/>
      <param name="RGBD/AngularUpdate" value="0.1"/>

      <!-- More conservative loop closure detection (optional) -->
      <param name="Rtabmap/LoopThr" value="0.08"/>

      <!-- Keep the robust error check that rejected bad closures -->
      <param name="RGBD/OptimizeMaxError" value="3.0"/>  <!-- already default -->
    </node>
  </group>
</launch>
